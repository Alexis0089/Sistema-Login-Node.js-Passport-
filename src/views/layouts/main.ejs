<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Login Nodejs</title>
        <link
            rel="stylesheet"
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css"
        />
    </head>
    <body>
        <nav class="navbar navbar-expand-lg bg-light">
            <div class="container-fluid">
                <a class="navbar-brand fw-bold fs-3" href="/">LOGIN</a>
                <button
                    class="navbar-toggler"
                    type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent"
                    aria-expanded="false"
                    aria-label="Toggle navigation"
                >
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div
                    class="collapse navbar-collapse d-flex flex-row my-3 justify-content-end"
                    id="navbarSupportedContent"
                >
                    <ul class="navbar-nav me-5">
                      <!-- Validamos, si existe usuario muestra las pestañas correctas -->
                      <% if (user) { %>
                          <li class="nav-item me-3">
                          <a class="nav-link" href="/profile">Profile</a>
                          </li>
                          <li class="nav-item me-3">
                          <a class="nav-link" href="/logout">Exit</a>
                          </li>
                      <% } else { %>
                      <li class="nav-item me-3">
                        <a class="nav-link" href="/signin">SignIN</a>
                        </li>
                        <li class="nav-item me-3">
                        <a class="nav-link" href="/signup">SignUP</a>
                        </li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>

        <div class="container">
            <!--  signupMessage -->
            <% if (signupMessage.length > 0) { %>
            <div class="alert alert-danger"><%= signupMessage %></div>
            <% } %>

            <!--  signinMessage -->
            <% if (signinMessage.length > 0) { %>
            <div class="alert alert-danger"><%= signinMessage %></div>
            <% } %> <%- body %>
        </div>
    </body>
</html>
